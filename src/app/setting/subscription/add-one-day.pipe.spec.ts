import { AddOneDayPipe } from './add-one-day.pipe';

describe('AddOneDayPipe', () => {
  it('create an instance', () => {
    const pipe = new AddOneDayPipe();
    expect(pipe).toBeTruthy();
  });
});
