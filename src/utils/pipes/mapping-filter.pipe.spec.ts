import { MappingFilterPipe } from './mapping-filter.pipe';

describe('MappingFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new MappingFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
